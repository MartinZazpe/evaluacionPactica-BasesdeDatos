Hola profes 😁

La unica cosa a tener en cuenta es:

en la db notes hay dos campos: can_delete la cual utiliza un TINYINT para poder indicar si la nota puede o no borrarse y 
por otro lado DELETED que permitira marcar la nota como borrada (hacer un soft delete) para poder filtrar luego.